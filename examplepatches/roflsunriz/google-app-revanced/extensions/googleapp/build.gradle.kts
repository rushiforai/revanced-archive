extension {
    name = "extensions/googleapp.rve"
}

android {
    namespace = "app.revanced.extension.googleapp"

    defaultConfig {
        minSdk = 23
    }
}

dependencies {
    testImplementation("junit:junit:4.13.2")
}
