# 変更履歴

このプロジェクトの重要な変更はこのファイルに記録します。書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従い、バージョンは Semantic Versioning に準拠します。

## [Unreleased]

### Fixed

- CI と Dependabot の分類の実行順が前後しても更新を取りこぼさないよう、同じ PR 番号と head SHA を再照合する経路を追加した。

### Changed

- 依存更新を安全に省力化するため、Dependabot の patch／minor PR を既存 CI の全チェック成功後に自動取り込みし、失敗ジョブを一度再実行する設定を追加した。

## [0.2.0] - 2026-09-05

Android・CYD実機間の通信と省電力下の定期送信は、接続実機がなく未検証です。PC側にはHTTPS証明書と読み書き別トークンの設定が必要です。

### Added

- CYDでトッピング期限と自動更新の状態を参照できるよう、PC向け認証付き状態中継APIとAndroidからのHTTPS送信・設定画面を追加した。読み書きのトークンを分離し、コードとpovo認証情報は送信しない。
- 通信停止や未確認の推定期限を見分けられるよう、受信鮮度、期限の取得元・観測時刻、残り秒数をAPIへ含めた。中継APIの認証・保存・入力検証のテストをCIへ追加した。
- PCで中継APIを導入できるよう、リリースにソースと手順書を含む `povo-cyd-relay.zip` を添付する。

### Fixed

- 最後のコード適用成功でも次の期限を推定保存し、実際の契約情報で確認できた期限へ補正できるようにした。
- 再起動やサービス停止後に過去の「更新中」を外部表示し続けないよう、実行中状態の復旧を追加した。

## [0.1.3] - 2026-08-31

### Fixed

- アプリ更新時に期限切れの旧アラームから起動したforeground serviceが、設定画面で将来の終了日時へ補正した後も長時間待機し続ける問題を修正した。
- 手動終了日時の更新、自動更新の一時停止、利用回数完了時に、進行中の境界監視serviceとWakeLockを直ちに終了するようにした。
- 終了12秒前まで5分を超えて残っている場合はforeground service内で待たず、正確なアラームへ戻してバッテリー消費を防ぐようにした。

## [0.1.2] - 2026-08-31

### Added

- 画面消灯中でも境界処理のCPU実行を維持できるように、foreground service中だけ最大20分のPartial WakeLockを追加した。
- 低速回線の切り分けができるように、コード・token・hostを出力せずcontroller解決、送信開始、境界前後、network validation、HTTP結果、watchdog timeoutを記録する診断ログを追加した。

### Changed

- トッピング終了後の128kbps回線でも送信を完遂しやすくするため、終了12秒前から安全な拒否を利用して接続を事前確立し、3秒間隔の再試行で終了境界をまたぐように変更した。
- 非同期API応答を最大60秒待つwatchdogを追加し、応答前の重複送信を防ぎながらtimeout後に再試行するように変更した。

### Fixed

- v0.1.0を追加済みの利用者が同じ読込エラーを繰り返さないように、失敗したsourceと旧RVPを削除し、v0.1.1以降へ追加し直す復旧手順をREADMEへ追記した。
- background起動時にpromo controllerが未解決のまま2秒再試行を繰り返していた問題を修正し、service側からKoin controllerを再解決するようにした。
- 同じ保存済みコードをpovo標準画面から手動適用した場合も成功回数と次回予約を同期するようにした。

## [0.1.1] - 2026-08-31

### Changed

- pull request自動ラベル付けを現行Node.jsランタイムで継続するため、`actions/labeler`をv7へ更新した。
- 初めて利用する人がManagerの入力欄やファイル形式で迷わないように、READMEをraw URL、ローカルRVP、APKM統合、通常版インストール、初回設定、トラブル対応の利用順へ再構成した。

### Fixed

- GitHub Release URLを`patches.json`の登録先として案内したため環境によって追加に失敗していた問題を修正し、Managerが直接取得できる`raw.githubusercontent.com`のsource URLを明記した。
- ReVanced ManagerがRVPをDEXコンテナとして読み込めるように、CIとReleaseで`buildAndroid`を実行し、`classes.dex`を含むAndroid用RVPだけを配布するよう修正した。

## [0.1.0] - 2026-08-31

### Added

- 168時間トッピングの終端で回線カバレッジが途切れる時間を最小化するため、実終了時刻の検出、正確なアラーム、foreground service、成功までの再試行を追加した。
- 途中利用から開始できるように、メール本文から同一プリペイドコードと入力期限を抽出し、サーバー側の残り利用可否を正として繰り返す処理を追加した。
- 認証情報を複製せずログイン済みセッションを利用し、失効時にコードを保留して再ログイン後に復旧する処理を追加した。
- プリペイドコードを端末内で保護するため、Android Keystore と AES-GCM による暗号化保存を追加した。
- Android 16 の AQUOS R8 pro を対象に、再起動復旧、正確なアラーム権限、バックグラウンド実行用コンポーネントを追加した。
- ReVanced Manager が取得できる `patches.json` と Patcher 22 対応 RVP のリリース基盤を追加した。
- 公式 povo 2.0 のアプリデータとログイン状態を保持したまま実機検証できるように、パッケージID、provider authority、アプリ固有 permission/action を衝突しない別IDへ変更する検証専用パッチを追加した。
- 設定場所が分からない状態を解消するため、ホーム画面へ状態付きの黄色い自動更新カードを常設し、メール登録・終了日時設定・一時停止へ直接移動できる導線を追加した。
- 契約APIが「残り○時間」だけを返す場合でも途中利用から開始できるように、初回の現在終了日時を設定する入力欄を追加した。
- 24回目の後に不要な再試行を行わないように、最大回数と現在利用中の回数を設定し、成功ごとに進捗を増やして最大到達時に停止する処理を追加した。
- 24時間・3日・7日など時間型のプリペイドトッピングへ対応できるように、メール本文から1回の有効時間を抽出し、必要なら時間単位で修正できる設定を追加した。
- 商品構成の変更に追従できるように、メール本文から商品種別、コードに紐づく回数、即時適用回数、有効時間、入力期限を抽出する汎用商品モデルと、旧設定の明示的な状態移行を追加した。

### Changed

- 初回設定で多数のボタンに迷わないように、メール・回数・終了日時を1つの主ボタンで保存し、設定済みの場合だけ一時停止と削除を表示する段階的な画面へ変更した。
- 単発コードの誤反復とまとめ商品の回数ずれを防ぐため、終端ごとの自動再適用を `repeatable_time_code` だけに限定し、購入直後の即時適用分とメールで届くコード回数を分離して数えるように変更した。
- GitHub ActionsのNode.js 20廃止警告を解消するため、checkout、Javaセットアップ、Gradleセットアップ、artifact upload、build provenanceを各公式の現行メジャーへ更新した。

### Fixed

- 7日24回分を前提にした欠損時の `24` 回・`168` 時間が別商品へ混入しないようにし、既存の7日24回分設定だけを同じ値のまま移行するよう修正した。
- メール末尾の販売予定日を入力期限と誤認しないように、「入力期限」ラベルに続く日時を優先して抽出するよう修正した。
- 登録中の反復コードとは別の単発コードを手入力した結果で利用回数が増えないように、自動送信または登録直後の送信と、その応答を1回ずつ対応付けるよう修正した。

### Security

- 長い入力による制御不能な再帰を防ぐため、ReVancedの推移依存で解決されていた脆弱な Apache Commons Lang 3.17.0 を、修正済みの3.20.0へ明示的に更新した（`GHSA-j288-q9x7-2f5v`）。

[Unreleased]: https://github.com/roflsunriz/povo-2.0-revanced/compare/v0.1.3...HEAD
[0.1.3]: https://github.com/roflsunriz/povo-2.0-revanced/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/roflsunriz/povo-2.0-revanced/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/roflsunriz/povo-2.0-revanced/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/roflsunriz/povo-2.0-revanced/releases/tag/v0.1.0
