# コントリビューション

## 開始前

1. `COMMON-AGENTS.md` と `AGENTS.md` を確認する。
2. issue と既存コードを調べ、同じ変更が進行中でないか確認する。
3. プロモコード、token、メール本文、電話番号などを fixture、ログ、画像へ含めない。

## 開発

- 現在の main を基に、小さく独立した変更にする。
- 難読化名へ依存せず、複数世代で共通する文字列・型・命令構造を指紋にする。
- 表示文言は `Strings` に集約し、日本語と主要言語を維持する。
- 状態遷移、再起動、認証失効、期限切れ、ネットワーク失敗を考慮する。
- 変更を `CHANGELOG.md` の `[Unreleased]` へ意図ベースで記録する。

## 必須検証

```powershell
./gradlew build
```

変更した指紋は、少なくともリポジトリにある全世代 APKM から抽出した base APK へ適用してください。実機検証結果、未実施項目、残るリスクを `verification.md` に記録します。

## コミット

日本語 Conventional Commits 形式を使います。

```text
feat(automation): 終了時刻に追従する再試行を追加
fix(parser): メール本文の日付誤検出を防止
```
