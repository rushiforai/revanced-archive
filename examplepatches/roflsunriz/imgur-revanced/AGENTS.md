# AGENTS.md

## 作業開始前の必須手順（最優先・例外なし）

1. エージェントは、調査、計画、コマンド実行、スキル利用、ファイル編集、コミット、プッシュを始める前に、必ずリポジトリ直下の `.\COMMON-AGENTS.md` を開き、先頭から末尾まで全文を読む。
2. `COMMON-AGENTS.md` はGit管理外のシンボリックリンクである。`git`や既定のignore設定が有効な`rg --files`の検索結果だけで、ファイルが存在しないと判断してはならない。PowerShellでは最初に次を実行する。

```powershell
Get-Content -Raw -LiteralPath .\COMMON-AGENTS.md
```

3. 読み取りに失敗した場合、出力が省略された場合、または末尾まで読めたことを確認できない場合は、一切の作業を開始せず、パスとシンボリックリンク先を確認して全文を再取得する。必要なら分割して末尾まで読む。
4. 全文を読了するまで、ローカル `AGENTS.md` だけを根拠に作業を続けてはならない。読了後は `COMMON-AGENTS.md` を最優先の指針とし、読了直後の最初の進捗報告で全文を読了したことを明示する。
   このファイルでは `imgur-revanced` 固有の補足だけを記載する。

## 目的
- Imgurアプリ用のReVanced patchを作る
- Postsの表示でデフォルトを"Public"ではなく"All"にする
- 下部広告欄の高さをゼロにし広告SDKの通信を全遮断する
- Postsの画像一覧表示時に画像長押しで該当ポストの格納1枚目直リンクをクリップボードにコピーする
- 画像詳細に入った後画像を長押ししてくるメニューの"Imgur Copy link"をダイレクトリンクに置き換える
- 同じく「テキストの共有」に表示されるタイトル + アルバムリンクもタイトル + 直リンクにする
- ポストに複数枚の画像が格納してあっても長押しした該当画像の直リンクにする
- Imgurアプリ内の設定に「Imgur ReVanced」を作りアルバムリンクコピー <-> 直リンクコピーを切替可能にする。デフォルトは直リンクコピー。
- 下のタブの「Discover」「Search」「Notifications」を非表示切替可能にする（デフォルトで非表示）。アイコンを非表示にしたとき、単に空間を空けるのではなく良い感じにスペースを取るようにデザインする。これもImgurアプリ内の設定で切替可能にする。

## 展開
- リリースワークフローを整備してリリースする
- 実機で検証する
- 検証結果と対策を記録する
- Imgurアプリのバージョンに依存せず適用できることを確認する
- ReVanced Managerで実際に適用できることを確認する

## Environment
<必要に応じて適宜書き足すこと。>