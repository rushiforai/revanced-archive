org.gradle.parallel = true
org.gradle.caching = true
kotlin.code.style = official

# Patch bundle version — embedded into the .rvp manifest ("Version" attribute).
# 1.0.0: scaffold + version-field smoke test.  1.1.0: "Spoof signature" (+ API-36 launch-crash fix).
# 1.3.0: "Voice reply file upload" (m4a/AAC remux + non-AAC transcode to AAC).
# 1.3.1: fix intermittent post-submit "error" — spoof PostWriteActivity's Activity-context cert read too.
# 1.4.0: "Remove advertisements"; PackageManager delegate regenerated for API 36 (DCInside 5.3.2 targets it).
# 1.5.0: "ReVanced settings" page (clickable ReVanced 패치 버전 row, patches register their own settings); voice reply single-pass transcode + m4a copy fast path + conversion progress bar + "원본 오디오 포맷 강제 사용" switch.
version = 1.5.0
