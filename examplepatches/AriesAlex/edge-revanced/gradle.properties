org.gradle.parallel = true
org.gradle.caching = true
kotlin.code.style = official
version = 0.1.0

# The upstream settings plugin declares a credentialed GitHub Packages
# repository even though this project resolves its dependencies publicly.
# Gradle requires non-empty values while configuring that unused repository.
githubPackagesUsername = unused
githubPackagesPassword = unused
